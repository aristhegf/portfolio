# WSL workaround: keep the heavy pnpm virtual store on the native
# Linux filesystem instead of the slow Windows mount (/mnt/c).
# Vercel ignores this file's effect on production (fresh install there).
virtual-store-dir=/home/omotayo/.cache/portfolio-vs
verify-deps-before-run=false
