# NOTE: do NOT set a custom virtual-store-dir or store-dir here. Moving the
# pnpm store outside the project root makes Astro's build miss its compile
# cache for .astro files inside node_modules (e.g. @sanity/astro's studio
# route) and fails the Vercel production build. Keep the default store.
verify-deps-before-run=false
